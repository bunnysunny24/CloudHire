{
  "name": "cloudhire",
  "version": "1.0.0",
  "private": true,
  "description": "Cloud-native interview management platform with React, Express, PostgreSQL, Redis, Docker, and AWS-ready configuration.",
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "lint": "npm run lint --workspace backend && npm run lint --workspace frontend",
    "test": "npm run test --workspace backend",
    "install:all": "npm install --workspaces"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
